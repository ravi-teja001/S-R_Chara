{
  "name": "s-r-migration",
  "private": true,
  "description": "Root package – Railway runs build/start from here and delegates to backend.",
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "web": "bash scripts/start-web.sh",
    "env:railway": "bash scripts/update-env-railway.sh",
    "build:web": "cd biochar-bloom-main && npm ci && npm run build",
    "build:web:prod": "cd biochar-bloom-main && npm ci && npm run build:prod",
    "deploy": "bash scripts/deploy.sh",
    "deploy:complete": "bash scripts/complete-deploy.sh",
    "deploy:all": "bash scripts/deploy-all.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
